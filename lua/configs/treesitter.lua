local options = {
  ensure_installed = { "javascript", "lua", "luadoc", "printf", "regex", "rust", "typescript", "vim", "vimdoc" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
